package ch.rasc.embeddedtc.runner;

import java.io.IOException;
import java.net.URISyntaxException;

public class M {

	public static void main(String[] args) throws URISyntaxException, NoSuchFieldException, SecurityException,
			IllegalArgumentException, IllegalAccessException, IOException {
		// URL url =
		// Runner.class.getProtectionDomain().getCodeSource().getLocation();
		// Path currentDir = Paths.get(url.toURI());
		// System.out.println(currentDir);
		//
		// System.out.println(System.getProperty("java.library.path"));
		//
		// //System.setProperty( "java.library.path", "/path/to/libs" );
		//
		// Field fieldSysPath = ClassLoader.class.getDeclaredField( "sys_paths"
		// );
		// fieldSysPath.setAccessible( true );
		// fieldSysPath.set( null, null );
		//
		
	}

}
